# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.eggs/
*.egg-info/

# Editors / OS
.idea/
.vscode/
*.swp

# Apple Numbers binaries (keep the repo text-only / diffable)
archive/yearly-analytics-pipeline/*.numbers

# Node / Observable Framework dashboard — dependencies & generated build output.
# Regenerate any time with:  cd dashboard && npm install && npm run build
node_modules/
dashboard/dist/
.observablehq/

# Transient ingest review files (regenerated by the sync scripts each sweep).
# The durable record of decisions is data/sync_ledger.csv, which IS tracked.
data/incoming/

# Generated fiscal-year export CSVs (scripts/export_added_items.py).
exports/
